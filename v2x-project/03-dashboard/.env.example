# Where the bridge server's WebSocket is reachable.
# Local dev: the bridge running on your PC.
# Deployed on Vercel for a live demo: your tunnel's wss:// URL (ngrok / Cloudflare Tunnel).
VITE_WS_URL=ws://localhost:8080/ws

# Where HazardApp's EventLogger is writing .jsonl files.
# Must match HAZARD_LOG_DIR set by your PowerShell batch scripts / omnetpp.ini run.
HAZARD_LOG_DIR=../results/live

# Port the bridge server listens on (dashboard connects to ws://<host>:<PORT>/ws)
PORT=8080
